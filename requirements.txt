blinker==1.9.0
charset-normalizer==3.4.4
click==8.3.1
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
Flask==3.1.2
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
fonttools==4.61.1
greenlet==3.3.1
itsdangerous==2.2.0
Jinja2==3.1.6
kiwisolver==1.4.9
MarkupSafe==3.0.3
matplotlib==3.10.8
numpy==2.4.2
packaging==26.0
pillow==12.1.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
reportlab==4.4.9
six==1.17.0
SQLAlchemy==2.0.46
typing_extensions==4.15.0
Werkzeug==3.1.5

В ходе разработки была проведена миграция базы данных от плоской структуры к реляционной. Это позволило нормализовать данные, исключить дублирование названий отделов и повысить производительность системы. Сейчас система наполнена актуальными тестовыми данными в соответствии с новой архитектурой.

запуск сервера

cd C:\vkr_project
venv\Scripts\activate
python app.py
http://127.0.0.1:5000/

остановка сервера
Ctrl + C 

ЕСЛИ СЕРВЕР НЕ ЗАПУСКАЕТСЯ!
# 1. Переходим в папку с вашим проектом (создайте её, если еще нет)
mkdir ~/vkr_project
cd ~/vkr_project

# 2. Создаем виртуальное окружение (venv)
py -m venv venv

# 3. Активируем его
source venv/bin/activate


Чтобы работа выглядела солидно, на защите сделайте акцент на следующих профессиональных аспектах, которые вы реализовали:

1. Архитектурные решения
Реляционная база данных: Вы используете связанные таблицы (staff, departments, results). Это показывает ваши навыки проектирования сложных структур данных.

Механизм Soft Delete (Архивация): Вместо физического удаления данных вы используете статус is_active. Это стандарт корпоративных систем, обеспечивающий сохранность истории даже после увольнения сотрудника.

Нормализация данных: Названия отделов хранятся в отдельном справочнике, что исключает дублирование и ошибки ввода.

2. Математический и аналитический аппарат
Автоматизация расчетов: Ваша система сама вычисляет итоговый балл и выносит вердикт («Лидер», «Профессионал» и т.д.) на основе алгоритмов в models.py.

Визуализация (Data Visualization): Генерация графиков компетенций в реальном времени — это «киллер-фича» для диплома. Это наглядное представление результатов анализа.

Мониторинг прогресса: Благодаря фиксации даты каждой оценки, вы реализовали возможность лонгитюдного анализа (отслеживания развития навыков сотрудника во времени).

3. Соответствие бизнес-требованиям (HR-логика)
Вы реализовали полный цикл управления данными (CRUD):
Формирование структуры (Отделы).
Учет кадрового состава (Сотрудники).
Проведение аудита компетенций (Оценка).
Хранение архива (История).

Теперь у меня есть полноценная информационная система, которая включает:

1. Многоуровневую авторизацию (Admin/Manager).
2. Управление структурой (Отделы) и персоналом.
3. Сложную бизнес-логику (Алгоритм оценки в models.py).
4. Визуализацию (Динамические графики компетенций).
5. Аналитический дашборд со сравнением сотрудников.
6. Генерацию документов (Экспорт в PDF).

«Система разработана с использованием виртуальных окружений (Virtual Environments). Это гарантирует, что даже если на сервере установлена устаревшая версия системного Python (например, без поддержки современных алгоритмов шифрования), наше приложение будет работать стабильно, используя собственный изолированный набор библиотек».

СЕРВЕР
«Приложение разработано на базе фреймворка Flask и использует встроенный WSGI-сервер для обработки HTTP-запросов. Это делает систему легковесной и мобильной: для её запуска не требуется установка громоздких серверных сред вроде MAMP или ServBay. Достаточно наличия интерпретатора Python и установленных зависимостей в виртуальном окружении».

Вопрос про «Человеческий фактор»: Ты заявляешь «минимизацию человеческого фактора». Комиссия спросит: «А если HR введет неверные данные специально?».

Твой ответ в стиле ЭИ: «Система внедряет прозрачные критерии (Facts vs Evaluations). Каждый балл должен быть обоснован конкретным индикатором, что делает предвзятость видимой для системы аудита».

Вопрос про SQLite3: Могут спросить, почему не PostgreSQL.

Ответ: «Для целей MVP и малого бизнеса, на который ориентирована работа, SQLite3 обеспечивает достаточную производительность при минимальных затратах на развертывание».

Почему используется сервер разработки?

Можно ответить так (запомни 1–2 предложения):

«В рамках ВКР приложение разрабатывалось и тестировалось с использованием встроенного сервера Flask, предназначенного для отладки. Для промышленного развертывания предполагается использование WSGI-сервера, например Gunicorn или uWSGI.»